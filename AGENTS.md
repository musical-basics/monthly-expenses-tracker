# Agent Notes

- After making repository changes, commit and push them to `origin/main` unless the user explicitly says to hold off.
- Keep unrelated pre-existing local edits out of commits unless the user asks to include them.
- Do not commit or print secrets from `.env`, `.env.local`, `data/`, or other private files.
